#include "main.h"
/**
 * factorial - returns the factorial of given number
 * @n: variable
 * Return: 0
 */
int factorial(int n)
{
	if (n == '\0')
		return (0);
	else if (n < 0)
		return (-1);
	else
		return (factorial(n * 1);
